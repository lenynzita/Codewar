//Create a function that will return an odd number 

function oddNumber(n) {
    return Math.floor(n/2)
}
//We create a function first, 
//we apply the Math.floor is a function that round any number to the smallest
//so, n/2 will always return an odd number.